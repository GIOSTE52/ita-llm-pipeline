#version: "3.8"

services:
  pipeline:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      #aggiungi path se vuoi usare dataset esterno
      - /home/user/external_data:/app/external_data:ro

      # Dati in input (read-only)
      - ./data:/app/data:ro
      # Output e logs (read-write)
      - ./output:/app/output
      - ./logs:/app/logs
      # Mappa la cache di HuggingFace/Datatrove sul volume persistente
      - hf_cache:/root/.cache/huggingface
    environment:
      - DATATROVE_COLORIZE_LOGS=1
      - DATATROVE_COLORIZE_LOG_FILES=1
    command: ["python3", "src/main.py", "--config", "configs/default.conf"]
    # command: ["python3", "src/main.py", "--config", "configs/user.conf"]

volumes:
  hf_cache:
