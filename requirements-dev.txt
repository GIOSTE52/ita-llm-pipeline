# Dipendenze di sviluppo e testing
# Installa con: pip install -r requirements-dev.txt

# Testing framework
pytest>=9.0.0
pytest-cov>=4.1.0          # Coverage reports
pytest-xdist>=3.8.0        # Esecuzione parallela dei test
pytest-timeout>=2.4.0      # Timeout per i test
pytest-rerunfailures>=16.0 # Riprova test falliti

# Type checking statico
mypy>=1.5.0

# Linting e formatting
ruff>=0.1.0

# Flag per aggiungere le dipendenze del progetto principale 
-r requirements.txt
